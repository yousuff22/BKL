#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
vector<int> shortestToChar(string s, char c) {
    vector<int> ans(s.length());
    int i = s.size() -1;
    int cnt;
    while(i >= 0) {
        
    }
}
int main() {
    string s = "loveleetcode";
    char c = 'e';
    shortestToChar(s,c);
    return 0;
}